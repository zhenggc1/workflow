name: action_1
run-name: ${{ github.actor }} is learning GitHub Actions
on: [push]
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    env:
      AUTH_NAME: ${{ secrets.MY_NAME }}
    steps:
      - run: echo "$AUTH_NAME"
